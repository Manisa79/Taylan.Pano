name: Gridly CI

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup .NET 10 SDK
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'
      - name: Restore
        run: dotnet restore Gridly.sln
      - name: Build
        run: dotnet build Gridly.sln -c Release --no-restore /p:CI=true
      - name: Quality checks
        shell: pwsh
        run: ./tools/QualityChecks/Gridly.QualityChecks.ps1
      - name: Pack
        run: dotnet pack src/Gridly/Gridly.csproj -c Release --no-build -o artifacts/nuget
      - name: Upload NuGet artifact
        uses: actions/upload-artifact@v4
        with:
          name: gridly-nuget
          path: artifacts/nuget/*.nupkg
